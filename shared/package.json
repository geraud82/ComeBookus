{
  "name": "comebookus-shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared types, constants and utilities for ComeBookUs",
  "main": "index.ts",
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "lint": "eslint . --ext .ts,.tsx"
  },
  "devDependencies": {
    "@types/node": "^20",
    "eslint": "^8",
    "typescript": "^5"
  }
}
